using Microsoft.AspNetCore.Authentication.JwtBearer; using Microsoft.EntityFrameworkCore; using Microsoft.IdentityModel.Tokens; using PersonalDigitalVault.Api.Data; using PersonalDigitalVault.Api.Interfaces.Repositories; using PersonalDigitalVault.Api.Interfaces.Services; using PersonalDigitalVault.Api.Middleware; using PersonalDigitalVault.Api.Repositories; using PersonalDigitalVault.Api.Security; using PersonalDigitalVault.Api.Services; using PersonalDigitalVault.Api.Storage; using System.Security.Claims; using System.Text; using System.Threading.RateLimiting; var builder = WebApplication.CreateBuilder(args); builder.Services .AddControllers(); builder.Services .AddHttpContextAccessor(); builder.Services .AddHttpClient(); builder.Services .AddDbContext<AppDbContext>( options => { options.UseSqlServer( builder.Configuration .GetConnectionString( "DefaultConnection" ) ); } ); builder.Services.AddScoped< IUserRepository, UserRepository>(); builder.Services.AddScoped< IFolderRepository, FolderRepository>(); builder.Services.AddScoped< IDocumentRepository, DocumentRepository>(); builder.Services.AddScoped< ICredentialRepository, CredentialRepository>(); builder.Services.AddScoped< IDocumentShareRepository, DocumentShareRepository>(); builder.Services.AddScoped< ISubscriptionRepository, SubscriptionRepository>(); builder.Services.AddScoped< IAuthService, AuthService>(); builder.Services.AddScoped< IProfileService, ProfileService>(); builder.Services.AddScoped< IFolderService, FolderService>(); builder.Services.AddScoped< IDocumentService, DocumentService>(); builder.Services.AddScoped< ICredentialService, CredentialService>(); builder.Services.AddScoped< ISearchService, SearchService>(); builder.Services.AddScoped< IAdminService, AdminService>(); builder.Services.AddScoped< IDashboardService, DashboardService>(); builder.Services.AddScoped< IEmailService, EmailService>(); builder.Services.AddScoped< IDocumentShareService, DocumentShareService>(); builder.Services.AddScoped< IPayPalService, PayPalService>(); builder.Services.AddScoped< ISubscriptionService, SubscriptionService>(); builder.Services.AddScoped< IPayPalWebhookService, PayPalWebhookService>(); builder.Services.AddScoped< IStripeService, StripeService>(); builder.Services.AddScoped< StripeSubscriptionSynchronizer>(); builder.Services.AddScoped< IStripeSubscriptionService, StripeSubscriptionService>(); builder.Services.AddScoped< IStripeWebhookService, StripeWebhookService>(); builder.Services.AddScoped< ISubscriptionEntitlementService, SubscriptionEntitlementService>(); builder.Services.AddScoped< IAdminSubscriptionService, AdminSubscriptionService>(); builder.Services.AddSingleton< DocumentShareTokenService>(); builder.Services.AddScoped< EmailOtpService>(); builder.Services.AddScoped< RegistrationTokenService>(); builder.Services.AddScoped< TotpService>(); builder.Services.AddScoped< TotpSecretProtector>(); builder.Services.AddScoped< MfaChallengeTokenService>(); builder.Services.AddSingleton< PasswordResetTokenService>(); builder.Services.AddSingleton< PasswordHasher>(); builder.Services.AddSingleton< JwtTokenGenerator>(); builder.Services.AddSingleton< AesEncryption>(); builder.Services.AddSingleton< FileHashService>(); builder.Services.AddScoped< CurrentUserService>(); builder.Services.AddSingleton< FileStorageService>(); builder.Services.AddSingleton< GoogleOAuthStateService>(); builder.Services.AddScoped< GoogleDriveClient>(); builder.Services.AddScoped< VaultBackupPackageService>(); builder.Services.AddScoped< IGoogleDriveBackupService, GoogleDriveBackupService>(); builder.Services.AddHostedService< GoogleDriveAutoBackupWorker>(); builder.Services.AddScoped< DbSeeder>(); string jwtKey = GetRequiredSetting( builder.Configuration, "Jwt:Key" ); string mfaTokenKey = GetRequiredSetting( builder.Configuration, "Security:MfaTokenKey" ); string jwtIssuer = GetRequiredSetting( builder.Configuration, "Jwt:Issuer" ); string jwtAudience = GetRequiredSetting( builder.Configuration, "Jwt:Audience" ); _ = GetRequiredSetting( builder.Configuration, "Security:MfaIssuer" ); _ = GetRequiredSetting( builder.Configuration, "Security:MfaAudience" ); if ( Encoding.UTF8 .GetByteCount( jwtKey ) < 32 ) { throw new InvalidOperationException( "Jwt:Key must contain at least 32 bytes." ); } if ( Encoding.UTF8 .GetByteCount( mfaTokenKey ) < 32 ) { throw new InvalidOperationException( "Security:MfaTokenKey must contain at least 32 bytes." ); } if ( string.Equals( jwtKey, mfaTokenKey, StringComparison.Ordinal ) ) { throw new InvalidOperationException( "Jwt:Key and Security:MfaTokenKey must be different." ); } builder.Services .AddAuthentication( JwtBearerDefaults .AuthenticationScheme ) .AddJwtBearer( options => { options.TokenValidationParameters = new TokenValidationParameters { ValidateIssuer = true, ValidIssuer = jwtIssuer, ValidateAudience = true, ValidAudience = jwtAudience, ValidateLifetime = true, ValidateIssuerSigningKey = true, IssuerSigningKey = new SymmetricSecurityKey( Encoding.UTF8 .GetBytes( jwtKey ) ), ClockSkew = TimeSpan.Zero }; options.Events = new JwtBearerEvents { OnTokenValidated = async context => { ClaimsPrincipal? principal = context.Principal; string? purpose = principal? .FindFirst( "purpose" )? .Value; if ( !string.Equals( purpose, "access", StringComparison.Ordinal ) ) { context.Fail( "Invalid token." ); return; } string? userIdText = principal? .FindFirst( ClaimTypes .NameIdentifier )? .Value; if ( !int.TryParse( userIdText, out int userId ) || userId <= 0 ) { context.Fail( "Invalid token." ); return; } IUserRepository userRepository = context .HttpContext .RequestServices .GetRequiredService< IUserRepository >(); var currentUser = await userRepository .GetByIdAsync( userId ); if ( currentUser == null || !currentUser.IsActive ) { context.Fail( "Invalid token." ); return; } string? tokenRole = principal? .FindFirst( ClaimTypes.Role )? .Value; bool validDatabaseRole = string.Equals( currentUser.Role, "User", StringComparison .OrdinalIgnoreCase ) || string.Equals( currentUser.Role, "Admin", StringComparison .OrdinalIgnoreCase ); if ( !validDatabaseRole || string.IsNullOrWhiteSpace( tokenRole ) || !string.Equals( tokenRole, currentUser.Role, StringComparison .OrdinalIgnoreCase ) ) { context.Fail( "Invalid token." ); return; } bool isNormalUser = string.Equals( currentUser.Role, "User", StringComparison .OrdinalIgnoreCase ); if (isNormalUser) { if ( !currentUser .IsEmailVerified ) { context.Fail( "Invalid token." ); return; } if ( !currentUser .IsTotpEnabled || string .IsNullOrWhiteSpace( currentUser .TotpSecretEncrypted ) ) { context.Fail( "Invalid token." ); return; } } } }; } ); builder.Services .AddAuthorization(); builder.Services .AddRateLimiter( options => { options.GlobalLimiter = PartitionedRateLimiter .Create< HttpContext, string >( httpContext => { if ( !HttpMethods .IsPost( httpContext .Request .Method ) ) { return RateLimitPartition .GetNoLimiter( "non-post-request" ); } string path = httpContext .Request .Path .Value? .ToLowerInvariant() ?? string.Empty; string ipAddress = httpContext .Connection .RemoteIpAddress? .ToString() ?? "unknown"; if ( path == "/api/auth/register" ) { return CreateFixedWindowPartition( $"{ipAddress}|register", 5, TimeSpan .FromMinutes( 10 ) ); } if ( path == "/api/auth/verify-email" ) { return CreateFixedWindowPartition( $"{ipAddress}|verify-email", 10, TimeSpan .FromMinutes( 5 ) ); } if ( path == "/api/auth/resend-email-otp" ) { return CreateFixedWindowPartition( $"{ipAddress}|resend-email", 5, TimeSpan .FromMinutes( 10 ) ); } if (path == "/api/auth/forgot-password") { return CreateFixedWindowPartition( $"{ipAddress}|forgot-password", permitLimit: 5, window: TimeSpan.FromMinutes(10)); } if (path == "/api/auth/reset-password") { return CreateFixedWindowPartition( $"{ipAddress}|reset-password", permitLimit: 5, window: TimeSpan.FromMinutes(10)); } if ( path == "/api/auth/setup-totp" ) { return CreateFixedWindowPartition( $"{ipAddress}|setup-totp", 10, TimeSpan .FromMinutes( 10 ) ); } if ( path == "/api/auth/verify-totp-setup" ) { return CreateFixedWindowPartition( $"{ipAddress}|verify-totp-setup", 5, TimeSpan .FromMinutes( 5 ) ); } if ( path == "/api/auth/login" ) { return CreateFixedWindowPartition( $"{ipAddress}|login", 5, TimeSpan .FromMinutes( 1 ) ); } if ( path == "/api/auth/verify-login-totp" ) { return CreateFixedWindowPartition( $"{ipAddress}|verify-login-totp", 5, TimeSpan .FromMinutes( 5 ) ); } return RateLimitPartition .GetNoLimiter( "non-auth-endpoint" ); } ); options.OnRejected = async ( context, cancellationToken ) => { context .HttpContext .Response .StatusCode = StatusCodes .Status429TooManyRequests; await context .HttpContext .Response .WriteAsJsonAsync( new { message = "Too many requests. Please try again later." }, cancellationToken ); }; } ); var app = builder.Build(); using ( var scope = app.Services .CreateScope() ) { DbSeeder seeder = scope .ServiceProvider .GetRequiredService< DbSeeder >(); await seeder .SeedAsync(); } app.UseMiddleware< ExceptionMiddleware>(); app.UseHttpsRedirection(); app.UseDefaultFiles(); app.UseStaticFiles(); app.UseRateLimiter(); app.UseAuthentication(); app.UseAuthorization(); app.MapControllers(); app.Run(); static string GetRequiredSetting( IConfiguration configuration, string key) { string? value = configuration[ key ]; if ( string.IsNullOrWhiteSpace( value ) ) { throw new InvalidOperationException( $"{key} is missing." ); } return value; } static RateLimitPartition<string> CreateFixedWindowPartition( string partitionKey, int permitLimit, TimeSpan window) { return RateLimitPartition .GetFixedWindowLimiter( partitionKey, _ => new FixedWindowRateLimiterOptions { PermitLimit = permitLimit, Window = window, QueueProcessingOrder = QueueProcessingOrder .OldestFirst, QueueLimit = 0, AutoReplenishment = true } ); }
